<h1 class="display-4">Tickets</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Tag</th>
            <th>Liste des tickets acceptés</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <% tickets.forEach(function(ticket){ %>
            <tr>
                <td><%= ticket.tag%></td>
                <td><%= ticket.createdAt%></td>
                <td><%= ticket.status%></td>
            </tr>
        <% }) %>
    </tbody>
</table>